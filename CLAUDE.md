# Bot-Inversiones

Sistema de inteligencia financiera para apoyo a decisiones de inversión (no un chatbot de bolsa). Ver `docs/00-arquitectura-conceptual.md` para la arquitectura conceptual del proyecto (Fase 0) y las fases de desarrollo acordadas.

## Protocolo obligatorio de privacidad

**Antes de tratar cualquier dato del usuario (identidad, cartera, holdings, plataformas, credenciales), lee y aplica `docs/00-protocolo-privacidad.md` en su totalidad.** Es un requisito permanente para todo el trabajo en este proyecto, no solo una referencia opcional. En resumen:

- Pseudonimizar por defecto: usar `CARTERA_A`, `CUENTA_01`, etc. en vez de identidad real cuando sea posible.
- Nunca solicitar ni almacenar contraseñas, claves API, seed phrases, tokens o credenciales de ningún tipo.
- Separar siempre identidad, información financiera y credenciales.
- Minimizar: pedir solo el dato estrictamente necesario; usar rangos en vez de cifras exactas cuando baste.
- Antes de enviar cualquier dato del usuario a un servicio externo, evaluar necesidad y minimizar lo transmitido.

## Estado del proyecto

- Fase 0 (arquitectura conceptual): completada — `docs/00-arquitectura-conceptual.md`.
- Fase 1 (definición de MVP, holdings, watchlist, fuentes de datos): completada — ver `cartera/` (holdings pseudonimizados de `CARTERA_A` y watchlist) y `docs/01-fase1-fuentes-datos-y-plantilla.md` / `docs/02-fase1-gaps-y-roadmap-fuentes.md`. Pendiente para más adelante (no ahora): investigar plataformas alternativas a Binance/Kraken/MyInvestor.
- Fase 2 (Fundamental Engine): cripto (`engine/crypto/`, v1 BTC/ETH/ADA/SOL/DOT/XRP — `informes/2026-07-20_cripto_fundamentales_v1.md`) y acciones (`engine/equity/`, v1 IBM/NVDA/XOM — `informes/2026-09-03_acciones_fundamentales_v1.md`) completas. Acciones usa el conector MCP de Alpha Vantage (cuenta del usuario) en vez de SEC EDGAR/Yahoo Finance directos — ver la nota de arquitectura en `engine/equity/README.md` (no se guarda ninguna clave de API en el repo, protocolo de privacidad). Pendiente: extender scoring consolidado (Fase 6) y razonamiento (Fase 7) a acciones, hoy solo cubren cripto.
- Fase 3 (Motor Técnico): v1 funcionando para BTC/ETH/ADA/SOL/DOT/XRP — `engine/technical/`, ver `informes/2026-07-22_cripto_tecnico_v1.md`. Principio de diseño: confluencia de indicadores, nunca señales aisladas.
- Fase 5 (Motor Macro): v1 funcionando para EE.UU. y Eurozona (FRED) — `engine/macro/`, ver `informes/2026-07-22_macro_y_sintesis_v1.md`. Régimen: EE.UU. "expansión con inflación pegajosa", Eurozona "pivote hawkish reciente del BCE". Contextualiza (no reemplaza) los hallazgos de fundamentales y técnico.
- Fase 4 (Noticias/sentimiento): v2 — `engine/news/`, ver `informes/2026-09-03_noticias_sentimiento_v2.md`. Fuente principal ahora es `NEWS_SENTIMENT` de Alpha Vantage (conector MCP, sentimiento por ticker ya calculado) en vez de GDELT (sigue bloqueado con 429 persistente en este entorno, código conservado). `tickers` filtra en modo "Y" — una llamada por ticker, no se puede batch-ear el watchlist completo sin gastar buena parte de la cuota diaria (25/día). Se investigaron RSS como alternativa (a petición del usuario): Google News RSS descartado (sus propios términos prohíben cualquier uso no personal), SEC EDGAR filings feed válido pero solo cubre presentaciones regulatorias, no noticias generales. Corrección importante hecha en esta v2: la Clarity Act de XRP NO estaba aprobada por el Senado pleno como se dijo en julio, solo por el Comité — el Senado la aplazó el 7 de agosto (`NEWS_FINDINGS` en `thesis.py` ya actualizado). Registro de personas influyentes (`engine/news/personas_influyentes.json`) con 13 identidades verificadas (Cava, Alfayate, Moro, Galán, Lacalle, Rallo, Profesor del Dividendo, Hatzius, Roubini, Soros, Dalio, Druckenmiller, Paul Tudor Jones, Burry) — ninguna con credibilidad asumida, se gana con histórico real registrado. Varios nombres recibidos del usuario (vía Gemini) quedaron sin añadir por ser solo apellido sin fuente, ver `pendientes_de_identificar` en el propio JSON. Conectado al Data Contract el 2026-09-03 (`engine/contract/adapters.py::adapt_news`, ver más abajo) — `data/news/{ID}.json`, una fila por artículo, sin agregar.
- Fase 6 (Scoring consolidado): v1 funcionando — `engine/scoring/consolidate.py`, ver `informes/2026-07-22_scoring_consolidado_v1.md`. Reúne los dominios por activo con su Confidence/Data Quality propios, sin fusionarlos en un único número (eso es explícitamente Fase 7, no construida). Deliberadamente no incluye un "score de compra/venta".
- Fase 7 (Motor de Razonamiento): v1 funcionando para cripto y acciones — `engine/reasoning/thesis.py`, ver `informes/2026-07-22_tesis_inversion_v1.md` (cripto) e `informes/2026-09-03_tesis_acciones_v1.md` (acciones, `build_thesis_equity`). Detecta convergencias/divergencias entre fundamental y técnico con reglas fijas y documentadas, hereda y propaga confianza reducida (DOT) y contradicciones (XRP: noticias; XOM: crecimiento anual fuerte vs. último trimestre fallado, detectado automáticamente). El Thesis Ledger solo cubre cripto todavía (acciones necesitarían serie de precios diaria para volatilidad histórica).
- Watchlist viva en Airtable (base "Watchlist Financiera", tabla "Watchlist"): 9 registros (6 cripto marcados "Comprado" — holdings reales de CARTERA_A sin cantidades ni importes, solo ticker/tesis; 3 acciones "Vigilando"). Cada fila enlaza al informe correspondiente en GitHub. Complementa, no sustituye, el Thesis Ledger (git-versionado) ni `cartera/` (pseudonimizado, con cantidades reales — eso nunca va a Airtable).
- Pruebas de humo (`tests/`, `python3 -m unittest discover -s tests -v`): cubren los 5 motores con fixtures de datos reales congelados (BTC/XRP), sin llamadas de red. Ejecutar antes de tocar un motor existente y después de añadir uno nuevo.
- Thesis Ledger (Fase 0, punto 21): v1 funcionando — `engine/reasoning/ledger.py`, registros en `engine/reasoning/ledger/*.jsonl` (SÍ versionado en git, a diferencia de `_data/`). Primera tirada registrada el 2026-09-02 para los 6 activos cripto, horizonte de evaluación 90 días, umbral de "movimiento significativo" derivado de la volatilidad histórica de cada activo (no un número fijo). Pendiente: recordatorio real a 90 días para ejecutar `evaluate_pending`.
- Nota operativa: las carpetas `_data/` de cada motor no persisten entre reinicios de sesión (son gitignored, regenerables con cada `fetch_data.py`) — si un motor falla con `FileNotFoundError`, primero reejecutar su `fetch_data.py` antes de investigar nada más.
- Conectores MCP disponibles en la cuenta del usuario (desde 2026-09-03): Alpha Vantage, Bigdata.com, Airtable (base "Watchlist Financiera" ya creada, sin registros todavía), Google Drive, Figma, Canva, Wix, Zapier. Alpha Vantage gratuito: 25 llamadas/día, ~1/seg — espaciar llamadas, nunca lanzarlas en paralelo. Ninguna clave de API de estos conectores se guarda en el repo (protocolo de privacidad); los motores que los usan (`engine/equity/`) documentan la ingesta como paso manual dentro de la sesión, no como `fetch_data.py` automatizado.
- Decisión de ejecución (aplica a toda fase de automatización futura, Fase 10): el sistema únicamente propone: el usuario ejecuta manualmente. Sin credenciales de trading en el sistema por ahora.
- Mapa de ruta visual del proyecto (artefacto publicado, redesplegar si se pide una versión actualizada): https://claude.ai/code/artifact/270c9640-d4f7-47d3-a090-240203a5dc23 (fuente: scratchpad de la sesión que lo creó, no versionada en el repo).
- Capa de visualización/acceso (nueva, transversal, no una de las 10 fases originales): planificación en `docs/03-arquitectura-visualizacion-y-acceso.md` (revisión 2, aprobada conceptualmente por el usuario el 2026-09-03). Decisión de arquitectura: Web App estática (React/Vite) en **Cloudflare Workers, modo assets estáticos** (no Pages — Cloudflare recomienda Workers para proyectos nuevos desde 2026, Pages en mantenimiento) + Cloudflare Access (privado, gratis hasta 50 usuarios) para consulta diaria, Power BI Desktop + My Workspace para análisis profundo — ambos leyendo el mismo Data Contract en ficheros, sin backend ni base de datos en el MVP. Alpha Vantage ofrece peticiones ilimitadas para proyectos open-source/educativos verificados (verificado en alphavantage.co/support) — acción pendiente del usuario (escribir a support@alphavantage.co), no asumida. **Implementado y autorizado**: Data Contract + adaptadores (`engine/contract/`), que traducen la salida de cada motor a dos formas comunes (métrica y tesis) con `data_as_of` separado de `retrieved_at` (para evitar look-ahead bias en el futuro backtesting) y una regla de privacidad aplicada en el propio validador (rechaza cualquier fila con datos de `cartera/`). `python3 engine/contract/build.py` genera `data/metrics/*.json` y `data/thesis/*.json` (SÍ versionado en git).
- Fase A (diagnóstico) + preparación de Power BI: `engine/contract/qa.py` (lee `data/`, PASS/FAIL en cinco bloques: esquema/privacidad/temporal/fuentes/DimAsset — ver `informes/2026-09-03_data_qa_v1.md`) y `docs/04-modelo-power-bi.md` (Fact/Dim tables, Power Query, modelo estrella, medidas DAX — solo para lo que existe de verdad en `data/`, nada inventado). Usuario autorizó los 4 pendientes el 2026-09-03, en orden: (1) historizar `build.py` — **hecho**: `data/metrics/{ID}.json` y `data/thesis/{ID}.json` ahora son append-only e idempotentes (clave lógica: `asset_id+domain+metric+data_as_of+source` para métricas, `asset_id+data_as_of` para tesis — nunca `retrieved_at`); macro ya no escribe `_macro.json`, EE.UU./Eurozona son activos propios (`US.json`/`EA.json`); (2) `DimAsset` — **hecho**: `data/assets/{ID}.json` (sin historizar, se sobrescribe — atributos estáticos), con `currency` documentada por activo (acciones: literal de Alpha Vantage `COMPANY_OVERVIEW`; cripto: `"EUR"` asignado por el sistema vía Kraken, no propiedad del activo; macro: USD/EUR por región) — resuelve el hallazgo EUR/USD que `qa.py` había detectado; la comprobación de "unidades incompatibles" de `qa.py` ahora compara por `(metric, asset_type)` en vez de solo `metric`. `qa.py` da PASS general con los 11 activos (`data/assets/*.json`: 6 cripto + 3 acciones + US/EA). (3) ampliar adaptadores equity/technical — **hecho**: `adapt_equity` añade `eps`, `profit_margin_pct`, `operating_margin_pct`, sorpresa de resultados (`earnings_beats_8q`/`earnings_misses_8q`/`earnings_surprise_avg_pct`/`earnings_surprise_last_pct`) y precio objetivo de analistas (`analyst_target_price`/`analyst_upside_pct`/`analyst_n_analistas`); `adapt_technical` añade `sma20`/`sma50`/`sma100`/`sma200`, `atr14`, `atr14_pct_precio`, `volatilidad_hist_30d_anualizada_pct` — todo ya calculado por los motores, sin tocar su lógica de cálculo. (4) adaptador de noticias — **hecho**: `adapt_news(symbol, asset_type)` en `engine/contract/adapters.py` traduce `NEWS_SENTIMENT` de Alpha Vantage (guardado a mano en `engine/news/_data/{ID}_news_sentiment.json`, sin `fetch_data.py` — mismo patrón manual que `engine/equity/`) a `data/news/{ID}.json`, historizado igual que métricas (clave lógica `asset_id+news_id`). Una fila por (artículo, activo), nunca un promedio agregado — `sentiment`/`relevance` vienen del bloque `ticker_sentiment` específico del activo, no del sentimiento general del artículo. `source_priority` usa la jerarquía periodística de `engine/news/sources.py` (1-5), distinta de la de fuentes de mercado. `persona_influyente` solo se rellena si el autor coincide literalmente con `personas_influyentes.json` — en la primera consulta real (XRP, 50 artículos) ninguno coincidió, campo `null` documentado como ausencia real. `qa.py` añadió un sexto bloque de validación (esquema/privacidad/duplicados/fuente de noticias). Los 4 pasos autorizados quedan completos: `qa.py` da PASS general (esquema, privacidad, temporal, fuentes, DimAsset, noticias), suite de tests en 48. Web App real sigue sin construirse.
- Power BI Desktop v1 (2026-09-03/04): construido y validado con el usuario, 6 páginas (Data Quality, Market Overview, Asset Research, Thesis, Macro, News & Sentiment) sobre el modelo estrella completo (`DimAsset`, `DimDate`, `FactMetrics`, `FactThesis`, `FactThesisFindings`, `FactNews`), 28 medidas DAX. Detalle completo en `informes/2026-09-03_data_contract_v1.1_y_powerbi_v1.md`. Automatización de cripto/macro vía GitHub Actions (`.github/workflows/actualizar-datos-libres.yml`, diario 06:00 UTC + manual, Kraken/CoinGecko/DefiLlama/FRED, sin credenciales) validada con 2 ejecuciones reales — solo commitea si `qa.py`+tests pasan y hay cambios reales (`_write_asset_row` ya no reescribe por solo `retrieved_at`). Corregido `fecha_dato` de BTC/XRP (`engine/technical/score.py`/`engine/crypto/score.py` usaban `datetime.now()` en vez de la fecha real del dato — commits `c89d875` código + `f89cf05` corrección de las 28 filas ya escritas con fecha incorrecta). Acciones/noticias siguen manuales (Alpha Vantage) — mensaje a soporte preparado en `informes/2026-09-04_mensaje_alpha_vantage_open_source.md`, no enviado.
- Backfill histórico (2026-09-04, `engine/contract/backfill.py`, distinto del cron incremental `build.py`): análisis de profundidad real por fuente en `informes/2026-09-04_estrategia_backfill_historico.md` (Kraken ~2 años tope duro, CoinGecko 365 días tope duro, DefiLlama y FRED ya con histórico completo capturado). Bloque 1 (macro/FRED) — **hecho**: `historical_series_us()`/`historical_series_ea()` en `engine/macro/score.py` reutilizan la misma fórmula de YoY que ya usaba `score_us()/score_ea()` aplicada a toda la serie, sin `regimen_estimado`/`señales` (síntesis de "hoy", no dato histórico). `adapt_macro_backfill()` en adapters.py, escribe por la misma `_write_metric_rows()` idempotente que el cron — EE.UU. desde 1948-01-01, Eurozona desde 1997-12-01, 12213 filas nuevas. Gap corregido (2026-09-04): `adapt_macro()` (incremental, no el backfill) usaba fecha de descarga como `data_as_of` en vez de la fecha real del dato — mismo patrón de bug que se corrigió para BTC/XRP, corregido reutilizando `historical_series_us()/ea()` (fecha real por métrica, ya no una única fecha compartida); corrección histórica aplicada (8 filas mal fechadas eliminadas de `US.json`/`EA.json`, sin necesidad de regenerarlas — el backfill del bloque 1 ya las tenía correctas). Bloque 2 (TVL DefiLlama) — **hecho**: `historical_tvl_percentile()` en `engine/crypto/score.py` generaliza `_pct_in_window()` (percentil en ventana móvil de 365 días, misma fórmula que ya usa el flujo incremental) a toda la serie de TVL ya descargada. `adapt_crypto_backfill()` en adapters.py, mismo `_write_metric_rows()` idempotente. Resultado: ETH desde 2018-02-12 (3127 filas), SOL desde 2021-03-26 (1989), ADA desde 2022-01-11 (1698) — 6814 filas nuevas; DOT da 0 filas por una incidencia de datos real y ya conocida (DefiLlama reporta TVL=0 en absolutamente todos los puntos de la serie de Polkadot, ya marcada por `posible_incidencia_datos` en el motor, no un fallo del backfill). Verificado idempotente. Bloque 3 (precio/OHLC/técnico cripto) — **hecho**: fuente nueva elegida tras comparar explícitamente Coinbase Exchange vs. Yahoo Finance (no se asumió Yahoo, pese a que en el análisis del bloque 1 parecía la candidata obvia) — **Coinbase Exchange** ganó por API oficial/documentada y ausencia de zona gris de términos de uso (mismo criterio ya aplicado al descartar Google News RSS), y por dar pares nativos en EUR (misma divisa que ya usa este motor vía Kraken, sin necesitar convertir con un tipo de cambio histórico); a cambio da algo menos de profundidad que Yahoo para ETH/XRP/ADA, aceptable porque el objetivo pedido era "varios meses/años", no el máximo posible. Binance descartado (bloqueado por restricción geográfica/ToS desde este entorno); CryptoCompare descartado (su API gratuita ya exige clave, rompe el patrón "sin clave" de todas las fuentes de este proyecto). `engine/technical/fetch_backfill.py` (fetcher nuevo) + `score.py::historical_series()` (misma metodología de `indicators.py` que ya usa `score_asset()`, recorriendo el array completo en vez de `[-1]`) + `_split_contiguous()` (huecos reales de calendario se tratan como tramos independientes, para que ninguna ventana móvil mezcle precios de antes y después de un hueco — hallazgo real: Coinbase deslistó XRP en EEUU 2021-01→2023-07 por el litigio SEC-Ripple, ~905 días de hueco). `adapt_technical_backfill()` en adapters.py, fuente `"Coinbase"` (nunca `"Kraken"`). Métrica nueva `volumen` (unidad `"unidades"`), añadida también al flujo incremental para no divergir. Resultado real (primera pasada): BTC desde 2015-04-23 (32938 filas), ETH desde 2017-05-23 (26210), ADA desde 2021-03-18 (12312), SOL desde 2021-06-17 (11402), DOT desde 2021-06-16 (11412), XRP desde 2019-02-26 (9894) — 104168 filas. **Corrección de hueco de proceso (2026-09-04, mismo día)**: el usuario detectó en Power BI un hueco real de ~2 años en las 6 series (ej. BTC 2024-07-29→2026-07-20) — causa: `adapt_technical_backfill()` excluía por una frontera fija (primera vela de la caché rotativa de Kraken), asumiendo que reflejaba lo ya escrito en `data/metrics/`, pero el cron incremental solo escribe un punto por ejecución, nunca toda esa ventana. Corregido: la exclusión ahora se basa en `_existing_technical_dates()` (lee `data/metrics/{symbol}.json` directamente — cualquier fecha ya presente, de cualquier fuente, se salta), y `fetch_backfill.py::update_cache()` extiende la caché local hasta hoy en vez de re-descargar todo el histórico cada vez ("no descargar de más"). Nueva herramienta de diagnóstico reutilizable: `engine/contract/backfill.py::detect_technical_gaps()` / `python3 backfill.py --gaps`. Resultado de la corrección: 44100 filas nuevas (BTC +7650, ETH +7200, ADA +7200, SOL +7200, DOT +7200, XRP +7650) — huecos restantes son todos reales y explicados (baja liquidez temprana en Coinbase para BTC/ETH; XRP deslistado de Coinbase EEUU 2021-01→2023-07 por el litigio SEC-Ripple, evaluado cerrarlo con Yahoo Finance como segunda fuente — precios verificados consistentes en ambos extremos, ~0.5-2.9% de diferencia — pero no aplicado, no se justificaba una tercera fuente solo para 2.4 años de un activo). Verificado idempotente, sin fechas con más de una fuente, `qa.py` PASS, suite en 72 tests. Pendiente: bloque 4 (equity vía Alpha Vantage).
